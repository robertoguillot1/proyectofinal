from django.apps import AppConfig

class DetallesordenesConfig(AppConfig):
    # Define the default auto field type for the application, which is a primary key type.
    default_auto_field = 'django.db.models.BigAutoField'
    # The name of the application, used for Django to reference this app
    name = 'Apps.detallesOrdenes'
